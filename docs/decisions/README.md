# Architecture Decision Records

One file per significant decision. Numbered, immutable once accepted — a
reversal is a **new** ADR that supersedes the old one, never an edit.

Write one when a choice would otherwise be re-litigated in three months, or
when a future reader would reasonably ask "why on earth is it like this?".

Format: Context → Decision → Consequences → Status.
