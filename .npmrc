# Metro (React Native's bundler) does not reliably resolve pnpm's strict
# symlinked node_modules layout. Hoisting avoids a whole class of
# "module not found only on device" bugs. Do not remove without testing
# a physical-device build of apps/mobile.
node-linker=hoisted
strict-peer-dependencies=false
